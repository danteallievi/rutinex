# Postgres (consumed by docker-compose.yml en la raíz)
POSTGRES_USER=rutinex
POSTGRES_PASSWORD=rutinex
POSTGRES_DB=rutinex
POSTGRES_PORT=5432

# El API consume sus propias vars desde `apps/api/.env` (ver
# `apps/api/.env.example`). Recordatorio: la más sensible es
# `JWT_ACCESS_SECRET`.
